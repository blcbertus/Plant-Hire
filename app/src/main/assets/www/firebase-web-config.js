/**
 * Web Firebase config (used inside the WebView SPA).
 *
 * In Firebase Console: Project settings → Your apps → add a Web app (</>) if you do not have one.
 * Copy the `firebaseConfig` object from the snippet and assign it below (replace null).
 *
 * This is separate from google-services.json (Android). The web API key is client-side by design.
 */
window.__BLC_FIREBASE_WEB_CONFIG__ = null;
